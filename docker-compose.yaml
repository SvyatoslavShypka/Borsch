version: "3.9"

services:
  web:
#    secrets: # secrets block only for 'web' service
#      - NOTE_DB_USER
#      - NOTE_DB_PASSWORD
    expose:
      - 2665
    ports:
      - 7778:7778
    image: borsch3:latest
#    entrypoint: "/bin/sh -c 'eureka_password=`cat /run/secrets/password` && echo $eureka_password'"
#    entrypoint: "/bin/sh -c 'set NOTE_DB_PASSWORD=`cat /run/secrets/NOTE_DB_P` && set NOTE_DB_USER=`cat /run/secrets/NOTE_DB_U`'"
#    entrypoint: "/bin/sh -c 'cat /run/secrets/NOTE_DB_PASSWORD'"
    environment:
      NOTE_DB_USER: /run/secrets/db_user
      NOTE_DB_PASSWORD: /run/secrets/db_pass
#      - spring.profiles.active=production
#    restart: always
    secrets:
      - db_user
      - db_pass
    build: .
secrets: # top level secrets block
  db_user:
    file: /NOTE_DB_USER.txt
  db_pass:
    file: /NOTE_DB_PASSWORD.txt
